[project]
name = "minWhisper"
version = "0.1.0"
description = "Minimal Whisper Inference"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "einops>=0.8.2",
    "numpy>=2.4.6",
    "openai-whisper>=20250625",
    "torch>=2.12.0",
]
